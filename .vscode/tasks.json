{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "options": {
        "env": {}
    },
    "tasks": [
        {
            "label": "Update Python Dependencies",
            "type": "shell",
            "command": "~/.virtualenvs/streak_analyzer/bin/pip install -r requirements.txt --upgrade && ~/.virtualenvs/streak_analyzer/bin/pip install -r requirements-dev.txt --upgrade"
        },
    ]
}
